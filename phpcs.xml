<?xml version="1.0"?>
<ruleset name="device-detector">
    <description>Device Detector Coding Standard (PHP 5.6 compatible)</description>

    <file>.</file>

    <exclude-pattern>vendor/</exclude-pattern>
    <exclude-pattern>Tests/</exclude-pattern>
    <exclude-pattern>misc/</exclude-pattern>
    <exclude-pattern>autoload.php</exclude-pattern>

    <rule ref="PSR1"/>
    <rule ref="PSR2"/>

    <!-- Disallow more than 120 chars per line -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120" />
            <property name="absoluteLineLimit" value="120" />
        </properties>
    </rule>

    <!-- Require spaces around string concatenating -->
    <rule ref="Squiz.Strings.ConcatenationSpacing">
        <properties>
            <property name="spacing" value="1" />
        </properties>
    </rule>

    <!-- Disallow is_null function -->
    <rule ref="Generic.PHP.ForbiddenFunctions">
        <properties>
            <property name="forbiddenFunctions" type="array">
                <element key="is_null" value="null"/>
            </property>
        </properties>
    </rule>
</ruleset>
